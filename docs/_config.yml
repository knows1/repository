collections:
  authors:
    output: true

defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

github_comments_repository: "knows1/repository"
github_comments_issueid: 1

url: "https://knows1.github.io/repository/"
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag


